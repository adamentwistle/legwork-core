---
description: Add a new project to the legwork queue.
---

Follow the legwork-tracker skill, Lifecycle section, to add a project.

What I told you about it: $ARGUMENTS

Steps:
1. Resolve the legwork repo: $LEGWORK_DIR if set, otherwise ~/legwork.
2. Ask me only for what you cannot infer (category, energy, repo path).
3. Create projects/<kebab-name>.md following the legwork-tracker File format
   spec (frontmatter + `## Next prompt` + `## Log`). Write one honest log
   line and mint a real first prompt, not a placeholder.
4. Rebuild the dashboard and confirm in two lines.
